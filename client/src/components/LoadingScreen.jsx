import { motion } from 'framer-motion';  export default function LoadingScreen() {   const containerVariants = {     hidden: { opacity: 0 },     visible: {       opacity: 1,       transition: {         staggerChildren: 0.1       }     }   };    const dotVariants = {     hidden: { y: 0, opacity: 0.5 },     visible: {       y: -20,       opacity: 1,       transition: {         duration: 0.6,         repeat: Infinity,         repeatType: 'reverse'       }     }   };    return (     <div className="fixed inset-0 z-50 flex items-center justify-center bg-gradient-to-br from-blue-50 to-cyan-50 dark:from-slate-900 dark:to-slate-800">       <div className="text-center">         {/* Logo */}         <motion.div           animate={{ rotate: 360 }}           transition={{ duration: 2, repeat: Infinity, ease: 'linear' }}           className="w-16 h-16 bg-gradient-to-br from-blue-600 to-cyan-400 rounded-lg flex items-center justify-center mx-auto mb-6"         >           <span className="text-white font-bold text-2xl">S</span>         </motion.div>          {/* Loading Text */}         <motion.h2           variants={containerVariants}           initial="hidden"           animate="visible"           className="text-2xl font-bold text-foreground mb-2"         >           SolarSafe         </motion.h2>          {/* Animated Dots */}         <motion.div           variants={containerVariants}           initial="hidden"           animate="visible"           className="flex justify-center gap-2 mt-4"         >           {[0, 1, 2].map((i) => (             <motion.div               key={i}               variants={dotVariants}               className="w-3 h-3 bg-gradient-to-r from-primary to-accent rounded-full"             />           ))}         </motion.div>          <p className="text-foreground/60 mt-6 text-sm">Loading your solar future...</p>       </div>     </div>   ); } 
